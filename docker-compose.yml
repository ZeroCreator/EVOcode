version: "3.8"

services:
  evocode:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: evocode-landing
    ports:
      - "80:80"
    restart: unless-stopped
    # Для HTTPS раскомментируйте volumes и добавьте certbot
    # volumes:
    #   - ./certbot/conf:/etc/letsencrypt
    #   - ./certbot/www:/var/www/certbot

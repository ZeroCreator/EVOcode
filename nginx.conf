server {
    listen 80;
    listen [::]:80;
    server_name zerocreator.ru www.zerocreator.ru;

    root /usr/share/nginx/html;
    index index.html index.htm;

    charset utf-8;

    # Gzip
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied expired no-cache no-store private auth;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;

    # Кэширование статики
    location ~* \.(css|js|png|jpg|jpeg|gif|svg|ico|woff|woff2|ttf|eot|webp)$ {
        expires 6M;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # Защитные заголовки
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # SPA fallback (для React/Vue/Astro)
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Скрываем версию Nginx
    server_tokens off;
}

# Поддомен evocode.zerocreator.ru
server {
    listen 80;
    listen [::]:80;
    server_name evocode.zerocreator.ru;

    root /usr/share/nginx/html;
    index index.html index.htm;

    charset utf-8;

    # Gzip сжатие
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied expired no-cache no-store private auth;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;

    # Кэширование статики
    location ~* \.(css|js|png|jpg|jpeg|gif|svg|ico|woff|woff2|ttf|eot|webp)$ {
        expires 6M;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # Защитные заголовки
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Скрываем версию Nginx
    server_tokens off;
}